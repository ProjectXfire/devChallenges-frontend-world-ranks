import { ObjectInfoPipe } from './object-info.pipe';

describe('ObjectInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
